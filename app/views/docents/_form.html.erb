<%= form_for(@docent) do |f| %>
  <% if @docent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@docent.errors.count, "error") %> prohibited this docent from being saved:</h2>

      <ul>
      <% @docent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :docentnumber %><br />
    <%= f.text_field :docentnumber %>
  </div>
  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
